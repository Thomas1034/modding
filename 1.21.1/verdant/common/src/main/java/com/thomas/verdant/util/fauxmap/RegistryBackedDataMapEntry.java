package com.thomas.verdant.util.fauxmap;

// Registry type, inner data
public class RegistryBackedDataMapEntry<T, I implements InnerData> {

    // Holds:
    //
    // RegistryReference
    // InnerData value

    public RegistryBackedDataMapEntry(RegistryReference<T> registryReference, InnerData innerData) {

    }
}
